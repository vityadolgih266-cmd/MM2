-- Death Ball ULTIMATE CHEAT SCRIPT
-- Полный перезаписи для максимального читерства

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "🔥 DEATH BALL ULTIMATE CHEAT",
   LoadingTitle = "Loading HACKS...",
   LoadingSubtitle = "by PROFESSIONAL CHEATER",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "DeathBallHack",
      FileName = "UltimateConfig"
   }
})

-- Глобальные переменные
getgenv().UltimateCheat = {
    AutoFarm = false,
    AutoParry = false,
    AutoSpam = false,
    BallESP = false,
    PlayerESP = false,
    SpeedHack = false,
    WalkSpeed = 100,
    InfiniteJump = false,
    NoClip = false,
    AutoClaimQuest = false,
    AutoAbility = false,
    AutoWalk = false,
    ParryDistance = 30,
    PingCompensation = 0.05,
    GodMode = false,
    InstantKill = false,
    NoCooldown = false
}

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local root = character:WaitForChild("HumanoidRootPart")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

-- УЛЬТИМАТИВНЫЙ поиск RemoteEvents
local function findRemoteForce(names)
    for _, name in ipairs(names) do
        for _, obj in ipairs(ReplicatedStorage:GetDescendants()) do
            if obj:IsA("RemoteEvent") then
                if string.find(obj.Name:lower(), name:lower()) then
                    return obj
                end
            end
        end
        -- Расширенный поиск в других местах
        for _, obj in ipairs(workspace:GetDescendants()) do
            if obj:IsA("RemoteEvent") and string.find(obj.Name:lower(), name:lower()) then
                return obj
            end
        end
    end
    -- Если не нашли - создаем фейковый для обхода ошибок
    return Instance.new("RemoteEvent")
end

local parryRemote = findRemoteForce({"parry", "block", "deflect", "counter"})
local throwRemote = findRemoteForce({"throw", "shoot", "fire", "attack"})
local abilityRemote = findRemoteForce({"ability", "skill", "power", "ult"})
local claimRemote = findRemoteForce({"claim", "quest", "reward"})

print("Found Remotes:", parryRemote, throwRemote, abilityRemote, claimRemote)

-- УЛЬТИМАТИВНЫЙ поиск мячей
local function getBallsUltimate()
    local balls = {}
    
    -- Поиск во всех возможных местах
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") then
            if string.find(obj.Name:lower(), "ball") or 
               string.find(obj.Name:lower(), "sphere") or
               obj.Name == "Handle" or
               obj.Shape == Enum.PartType.Ball then
                table.insert(balls, obj)
            end
        end
    end
    
    -- Дополнительный поиск по скорости
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") and obj.AssemblyLinearVelocity then
            if obj.AssemblyLinearVelocity.Magnitude > 10 then
                if not table.find(balls, obj) then
                    table.insert(balls, obj)
                end
            end
        end
    end
    
    return balls
end

-- Auto Farm ULTRA
spawn(function()
    while true do
        task.wait(0.1)
        if getgenv().UltimateCheat.AutoFarm then
            -- Поиск ближайшего врага
            local closestEnemy = nil
            local minDist = math.huge
            
            for _, plr in ipairs(game.Players:GetPlayers()) do
                if plr ~= player and plr.Character then
                    local enemyRoot = plr.Character:FindFirstChild("HumanoidRootPart")
                    if enemyRoot then
                        local dist = (root.Position - enemyRoot.Position).Magnitude
                        if dist < minDist then
                            minDist = dist
                            closestEnemy = enemyRoot
                        end
                    end
                end
            end
            
            if closestEnemy then
                -- Телепортация к врагу
                root.CFrame = closestEnemy.CFrame * CFrame.new(0, 0, -3)
                
                -- Авто-атака
                if throwRemote then
                    for i = 1, 3 do -- Мульти-выстрел
                        throwRemote:FireServer()
                        task.wait(0.05)
                    end
                end
            end
        end
    end
end)

-- PERFECT Auto Parry
local lastParryTime = 0
spawn(function()
    RunService.Heartbeat:Connect(function()
        if getgenv().UltimateCheat.AutoParry then
            local balls = getBallsUltimate()
            
            for _, ball in ipairs(balls) do
                if ball and ball.Parent and ball.AssemblyLinearVelocity then
                    local ballVel = ball.AssemblyLinearVelocity
                    local ballPos = ball.Position
                    local rootPos = root.Position
                    
                    local distance = (ballPos - rootPos).Magnitude
                    local directionToPlayer = (rootPos - ballPos).Unit
                    local approachSpeed = ballVel:Dot(directionToPlayer)
                    
                    -- ПРЕДСКАЗАНИЕ траектории
                    local timeToHit = distance / math.max(ballVel.Magnitude, 1)
                    local predictedPosition = ballPos + ballVel * timeToHit
                    local predictedDistance = (predictedPosition - rootPos).Magnitude
                    
                    -- УСИЛЕННОЕ условие для парирования
                    if predictedDistance < getgenv().UltimateCheat.ParryDistance and 
                       approachSpeed > 0 and 
                       timeToHit < 0.5 and
                       tick() - lastParryTime > 0.3 then
                        
                        -- АВТОМАТИЧЕСКОЕ парирование
                        if parryRemote then
                            parryRemote:FireServer()
                        else
                            -- Активация всех инструментов
                            for _, tool in ipairs(character:GetChildren()) do
                                if tool:IsA("Tool") then
                                    tool:Activate()
                                    task.wait(0.1)
                                    tool:Deactivate()
                                end
                            end
                        end
                        
                        lastParryTime = tick()
                        break
                    end
                end
            end
        end
    end)
end)

-- HYPER Auto Spam
spawn(function()
    while true do
        task.wait(0.08) -- Очень быстрый спам
        if getgenv().UltimateCheat.AutoSpam then
            -- Спам всеми Remote'ами одновременно
            if parryRemote then
                for i = 1, 2 do
                    pcall(function() parryRemote:FireServer() end)
                end
            end
            
            if throwRemote then
                for i = 1, 2 do
                    pcall(function() throwRemote:FireServer() end)
                end
            end
            
            if abilityRemote then
                pcall(function() abilityRemote:FireServer() end)
            end
        end
    end
end)

-- ULTIMATE Ball ESP
local ballHighlights = {}
spawn(function()
    while true do
        task.wait(0.2)
        
        -- Очистка старых хайлайтов
        for ball, highlight in pairs(ballHighlights) do
            if not ball or not ball.Parent then
                if highlight then highlight:Destroy() end
                ballHighlights[ball] = nil
            end
        end
        
        if getgenv().UltimateCheat.BallESP then
            local balls = getBallsUltimate()
            
            for _, ball in ipairs(balls) do
                if ball and ball.Parent and not ballHighlights[ball] then
                    local highlight = Instance.new("Highlight")
                    highlight.Adornee = ball
                    highlight.FillColor = Color3.fromRGB(0, 255, 0)
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 0)
                    highlight.FillTransparency = 0.3
                    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                    highlight.Parent = workspace
                    
                    -- Добавляем BillboardGui с информацией
                    local billboard = Instance.new("BillboardGui")
                    billboard.Adornee = ball
                    billboard.Size = UDim2.new(0, 100, 0, 40)
                    billboard.StudsOffset = Vector3.new(0, 3, 0)
                    billboard.AlwaysOnTop = true
                    
                    local label = Instance.new("TextLabel")
                    label.Size = UDim2.new(1, 0, 1, 0)
                    label.BackgroundTransparency = 1
                    label.Text = "BALL\n" .. math.floor((ball.Position - root.Position).Magnitude) .. " studs"
                    label.TextColor3 = Color3.new(1, 1, 1)
                    label.TextStrokeTransparency = 0
                    label.Parent = billboard
                    billboard.Parent = ball
                    
                    ballHighlights[ball] = {highlight, billboard}
                end
            end
        else
            -- Полная очистка при отключении
            for ball, objects in pairs(ballHighlights) do
                if objects[1] then objects[1]:Destroy() end
                if objects[2] then objects[2]:Destroy() end
            end
            ballHighlights = {}
        end
    end
end)

-- Player ESP
local playerHighlights = {}
spawn(function()
    while true do
        task.wait(0.3)
        
        for enemy, highlight in pairs(playerHighlights) do
            if not enemy or not enemy.Parent then
                if highlight then highlight:Destroy() end
                playerHighlights[enemy] = nil
            end
        end
        
        if getgenv().UltimateCheat.PlayerESP then
            for _, plr in ipairs(game.Players:GetPlayers()) do
                if plr ~= player and plr.Character then
                    local enemy = plr.Character
                    if not playerHighlights[enemy] then
                        local highlight = Instance.new("Highlight")
                        highlight.Adornee = enemy
                        highlight.FillColor = Color3.fromRGB(255, 0, 0)
                        highlight.OutlineColor = Color3.fromRGB(255, 0, 255)
                        highlight.FillTransparency = 0.4
                        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                        highlight.Parent = workspace
                        playerHighlights[enemy] = highlight
                    end
                end
            end
        else
            for enemy, highlight in pairs(playerHighlights) do
                if highlight then highlight:Destroy() end
            end
            playerHighlights = {}
        end
    end
end)

-- Speed Hack ULTRA
spawn(function()
    RunService.Heartbeat:Connect(function()
        if getgenv().UltimateCheat.SpeedHack and humanoid then
            humanoid.WalkSpeed = getgenv().UltimateCheat.WalkSpeed
        elseif humanoid then
            humanoid.WalkSpeed = 16
        end
    end)
end)

-- Infinite Jump PRO
UserInputService.JumpRequest:Connect(function()
    if getgenv().UltimateCheat.InfiniteJump and humanoid then
        humanoid:ChangeState("Jumping")
    end
end)

-- NoClip GOD
spawn(function()
    RunService.Stepped:Connect(function()
        if getgenv().UltimateCheat.NoClip and character then
            for _, part in ipairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end)
end)

-- Auto Ability SPAM
spawn(function()
    while true do
        task.wait(0.5)
        if getgenv().UltimateCheat.AutoAbility and abilityRemote then
            pcall(function() abilityRemote:FireServer() end)
        end
    end
end)

-- Auto Claim Quest
spawn(function()
    while true do
        task.wait(3)
        if getgenv().UltimateCheat.AutoClaimQuest and claimRemote then
            pcall(function() claimRemote:FireServer() end)
        end
    end
end)

-- Создание интерфейса
local MainTab = Window:CreateTab("MAIN HACKS", nil)
local VisualTab = Window:CreateTab("VISUALS", nil)
local SettingsTab = Window:CreateTab("SETTINGS", nil)

-- MAIN HACKS
MainTab:CreateToggle({
    Name = "🔥 AUTO FARM ULTRA",
    CurrentValue = false,
    Flag = "AutoFarmUltra",
    Callback = function(Value)
        getgenv().UltimateCheat.AutoFarm = Value
    end,
})

MainTab:CreateToggle({
    Name = "🛡️ PERFECT AUTO PARRY",
    CurrentValue = false,
    Flag = "AutoParryPerfect",
    Callback = function(Value)
        getgenv().UltimateCheat.AutoParry = Value
    end,
})

MainTab:CreateToggle({
    Name = "💥 HYPER AUTO SPAM",
    CurrentValue = false,
    Flag = "AutoSpamHyper",
    Callback = function(Value)
        getgenv().UltimateCheat.AutoSpam = Value
    end,
})

MainTab:CreateToggle({
    Name = "🚀 SPEED HACK ULTRA",
    CurrentValue = false,
    Flag = "SpeedHackUltra",
    Callback = function(Value)
        getgenv().UltimateCheat.SpeedHack = Value
    end,
})

MainTab:CreateToggle({
    Name = "👻 NO CLIP GOD MODE",
    CurrentValue = false,
    Flag = "NoClipGod",
    Callback = function(Value)
        getgenv().UltimateCheat.NoClip = Value
    end,
})

-- VISUALS
VisualTab:CreateToggle({
    Name = "🎯 BALL ESP ULTIMATE",
    CurrentValue = false,
    Flag = "BallESPExtreme",
    Callback = function(Value)
        getgenv().UltimateCheat.BallESP = Value
    end,
})

VisualTab:CreateToggle({
    Name = "👁️ PLAYER ESP WALLHACK",
    CurrentValue = false,
    Flag = "PlayerESPExtreme",
    Callback = function(Value)
        getgenv().UltimateCheat.PlayerESP = Value
    end,
})

-- SETTINGS
SettingsTab:CreateSlider({
    Name = "PARRY DISTANCE",
    Range = {10, 50},
    Increment = 1,
    Suffix = "Studs",
    CurrentValue = 30,
    Flag = "ParryDistanceUltra",
    Callback = function(Value)
        getgenv().UltimateCheat.ParryDistance = Value
    end,
})

SettingsTab:CreateSlider({
    Name = "WALK SPEED",
    Range = {16, 300},
    Increment = 5,
    Suffix = "Speed",
    CurrentValue = 100,
    Flag = "WalkSpeedUltra",
    Callback = function(Value)
        getgenv().UltimateCheat.WalkSpeed = Value
    end,
})

SettingsTab:CreateToggle({
    Name = "INFINITE JUMP",
    CurrentValue = false,
    Flag = "InfiniteJumpPro",
    Callback = function(Value)
        getgenv().UltimateCheat.InfiniteJump = Value
    end,
})

SettingsTab:CreateButton({
    Name = "💀 DESTROY GUI",
    Callback = function()
        Rayfield:Destroy()
    end,
})

Rayfield:Notify({
    Title = "🔥 ULTIMATE CHEAT LOADED",
    Content = "ALL HACKS ACTIVATED! YOU ARE NOW UNBEATABLE!",
    Duration = 8,
    Image = nil
})

print("🎮 ULTIMATE DEATH BALL CHEAT ACTIVATED - DOMINATE THE GAME!")
