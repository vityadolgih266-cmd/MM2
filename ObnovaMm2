-- 🔥 DEATH BALL GOD MODE CHEAT - FIXED VERSION
-- 🎯 САМЫЙ МОЩНЫЙ ЧИТ ДЛЯ DEATH BALL

-- АЛЬТЕРНАТИВНАЯ БИБЛИОТЕКА UI (ГАРАНТИРОВАННО РАБОЧАЯ)
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wall%20v3"))()

-- 🎮 ОСНОВНЫЕ НАСТРОЙКИ
getgenv().GodMode = {
    AutoParry = false,
    AutoAttack = false, 
    PlayerESP = false,
    BallESP = false,
    Tracers = false,
    Boxes = false,
    Speed = false,
    WalkSpeed = 120,
    Fly = false,
    FlySpeed = 80,
    NoClip = false,
    GodMode = false,
    ParryDistance = 25,
    AttackSpeed = 0.1
}

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- СОЗДАЕМ ИНТЕРФЕЙС КОТОРЫЙ ТОЧНО БУДЕТ ВИДЕН
local Window = Library:CreateWindow("💀 DEATH BALL GOD MODE")

-- 🎯 АВТОМАТИЧЕСКОЕ ОБНАРУЖЕНИЕ ИГРЫ
local function DetectGame()
    local gameData = {
        Balls = {},
        Remotes = {},
        Tools = {}
    }
    
    -- Поиск мячей
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") and (string.find(obj.Name:lower(), "ball") or obj.Shape == Enum.PartType.Ball) then
            table.insert(gameData.Balls, obj)
        end
    end
    
    -- Поиск RemoteEvents
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("RemoteEvent") then
            local name = obj.Name:lower()
            if string.find(name, "throw") or string.find(name, "shoot") then
                gameData.Remotes.Throw = obj
            elseif string.find(name, "parry") or string.find(name, "block") then
                gameData.Remotes.Parry = obj
            end
        end
    end
    
    return gameData
end

local Game = DetectGame()

-- 🎮 ОСНОВНЫЕ ПЕРЕМЕННЫЕ
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local root = character:WaitForChild("HumanoidRootPart")

-- ⚡ СИСТЕМА АВТОПАРИРОВАНИЯ
local lastParry = 0
RunService.Heartbeat:Connect(function()
    if getgenv().GodMode.AutoParry then
        for _, ball in pairs(Game.Balls) do
            if ball and ball.Parent and ball.AssemblyLinearVelocity then
                local distance = (ball.Position - root.Position).Magnitude
                local ballVel = ball.AssemblyLinearVelocity
                
                if distance < getgenv().GodMode.ParryDistance and ballVel.Magnitude > 10 and tick() - lastParry > 0.2 then
                    -- Парирование
                    if Game.Remotes.Parry then
                        Game.Remotes.Parry:FireServer()
                    end
                    lastParry = tick()
                    break
                end
            end
        end
    end
end)

-- 🎯 СИСТЕМА АВТО-АТАКИ
local lastAttack = 0
RunService.Heartbeat:Connect(function()
    if getgenv().GodMode.AutoAttack and tick() - lastAttack > getgenv().GodMode.AttackSpeed then
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= player and plr.Character then
                local target = plr.Character
                if target:FindFirstChild("HumanoidRootPart") then
                    -- Атака
                    if Game.Remotes.Throw then
                        Game.Remotes.Throw:FireServer()
                    end
                    break
                end
            end
        end
        lastAttack = tick()
    end
end)

-- 👁️ ESP СИСТЕМА
local ESPFolder = Instance.new("Folder")
ESPFolder.Name = "DeathBallESP"
ESPFolder.Parent = workspace

RunService.Heartbeat:Connect(function()
    -- Очистка ESP
    for _, obj in pairs(ESPFolder:GetChildren()) do
        obj:Destroy()
    end
    
    -- Player ESP
    if getgenv().GodMode.PlayerESP then
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= player and plr.Character then
                local char = plr.Character
                local rootPart = char:FindFirstChild("HumanoidRootPart")
                
                if rootPart then
                    -- Бокс
                    if getgenv().GodMode.Boxes then
                        local box = Instance.new("BoxHandleAdornment")
                        box.Adornee = rootPart
                        box.Size = Vector3.new(4, 6, 2)
                        box.Color3 = Color3.new(1, 0, 0)
                        box.Transparency = 0.3
                        box.AlwaysOnTop = true
                        box.Parent = ESPFolder
                    end
                    
                    -- Трейсер
                    if getgenv().GodMode.Tracers then
                        local beam = Instance.new("Beam")
                        beam.Attachment0 = Instance.new("Attachment")
                        beam.Attachment0.Parent = root
                        beam.Attachment1 = Instance.new("Attachment") 
                        beam.Attachment1.Parent = rootPart
                        beam.Color = ColorSequence.new(Color3.new(1, 0, 0))
                        beam.Width0 = 0.2
                        beam.Width1 = 0.2
                        beam.Parent = ESPFolder
                    end
                end
            end
        end
    end
    
    -- Ball ESP
    if getgenv().GodMode.BallESP then
        for _, ball in pairs(Game.Balls) do
            if ball and ball.Parent then
                local highlight = Instance.new("Highlight")
                highlight.Adornee = ball
                highlight.FillColor = Color3.new(1, 1, 0)
                highlight.OutlineColor = Color3.new(1, 0, 0)
                highlight.FillTransparency = 0.2
                highlight.Parent = ESPFolder
            end
        end
    end
end)

-- 🚀 СИСТЕМА ПЕРЕДВИЖЕНИЯ
RunService.Heartbeat:Connect(function()
    -- Speed
    if getgenv().GodMode.Speed and humanoid then
        humanoid.WalkSpeed = getgenv().GodMode.WalkSpeed
    elseif humanoid then
        humanoid.WalkSpeed = 16
    end
    
    -- NoClip
    if getgenv().GodMode.NoClip and character then
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
    
    -- God Mode
    if getgenv().GodMode.GodMode and humanoid then
        humanoid.MaxHealth = math.huge
        humanoid.Health = math.huge
    end
end)

-- 🎮 СОЗДАЕМ ИНТЕРФЕЙС
local CombatTab = Window:CreateFolder("⚔️ COMBAT")
local VisualTab = Window:CreateFolder("👁️ VISUALS") 
local MovementTab = Window:CreateFolder("🚀 MOVEMENT")

-- ⚔️ COMBAT
CombatTab:Toggle("🛡️ AUTO PARRY", function(state)
    getgenv().GodMode.AutoParry = state
end)

CombatTab:Toggle("💥 AUTO ATTACK", function(state)
    getgenv().GodMode.AutoAttack = state
end)

CombatTab:Slider("PARRY DISTANCE", {min = 10, max = 50}, function(value)
    getgenv().GodMode.ParryDistance = value
end)

CombatTab:Slider("ATTACK SPEED", {min = 0.05, max = 1}, function(value)
    getgenv().GodMode.AttackSpeed = value
end)

-- 👁️ VISUALS
VisualTab:Toggle("👁️ PLAYER ESP", function(state)
    getgenv().GodMode.PlayerESP = state
end)

VisualTab:Toggle("🎯 BALL ESP", function(state)
    getgenv().GodMode.BallESP = state
end)

VisualTab:Toggle("📏 TRACERS", function(state)
    getgenv().GodMode.Tracers = state
end)

VisualTab:Toggle("🟦 BOX ESP", function(state)
    getgenv().GodMode.Boxes = state
end)

-- 🚀 MOVEMENT
MovementTab:Toggle("🚀 SPEED HACK", function(state)
    getgenv().GodMode.Speed = state
end)

MovementTab:Slider("WALK SPEED", {min = 16, max = 500}, function(value)
    getgenv().GodMode.WalkSpeed = value
end)

MovementTab:Toggle("👻 NO CLIP", function(state)
    getgenv().GodMode.NoClip = state
end)

MovementTab:Toggle("🛡️ GOD MODE", function(state)
    getgenv().GodMode.GodMode = state
end)

-- 🔧 UTILITIES
local UtilTab = Window:CreateFolder("🔧 UTILITIES")

UtilTab:Button("🔄 ОБНОВИТЬ ДАННЫЕ", function()
    Game = DetectGame()
end)

UtilTab:Button("💀 УДАЛИТЬ ГУЙ", function()
    Library:Destroy()
    ESPFolder:Destroy()
end)

print("✅ DEATH BALL GOD MODE АКТИВИРОВАН!")
print("🔥 Интерфейс должен быть виден сейчас!")
